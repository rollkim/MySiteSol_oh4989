---
date: 2026-08-06
project: oh4989
---

# 작업 로그 — 2026-08-06

> 세션 범위: 2026-08-05 저녁 ~ 08-06 오전. 프로젝트 착수부터 **운영 사이트 첫 배포 성공**까지.

---

## 작업 #1 — 프로젝트 착수 · 규칙 문서 정립

| 항목 | 내용 |
|------|------|
| 작업 유형 | 신규 구축 (Phase 0-1) |
| 대상 파일 | `oh4989/CLAUDE.md`, `oh4989/AGENTS.md`, `C:\_Hope\Ohsite\_핸드오프\*` |

### 작업 내용

빈 프로젝트 폴더에서 시작. PaRaSOL의 `CLAUDE.md` RULE 세트를 oh4989에 맞게 이식하고, 핸드오프 문서 3종(SPEC·디자인가이드·확정안 프로토타입)을 **프로젝트 폴더 밖**(`C:\_Hope\Ohsite\_핸드오프\`)에 배치했다. 17MB 확정안 HTML이 저장소·배포 산출물에 섞이지 않게 하기 위함.

### 변경 요약

- RULE-1~10·12~14는 경로만 바꿔 이식
- **RULE-11을 oh4989 도메인 규약으로 전면 교체** — 비회원 전용, `detailAddress` strip, 만원 단위 정수, DB ENUM 금지, `property_logs` 필수, 지도 `ssr:false`, 크롤링 금지, 법정 표기, 과장문구 금지
- CLAUDE.md에 **`[SPEC 수정 이력]` 섹션 신설** — 핸드오프 폴더의 SPEC 원문과 충돌 시 이 표가 우선한다고 명시

### 이슈 / 특이사항

`C:\_Dev\_md_doc\conlab\projects\ocy\info.md`가 **서버 root 비밀번호 평문 파일**이었다. 실수로 읽은 즉시 사용자에게 알리고 RULE-1 접근 금지 목록에 등재. SSH 키 인증 전환 후 비밀번호 교체와 파일 삭제를 권고함.

---

## 작업 #2 — 이식 대상 실측 검증 (SPEC 수정 4건 확정)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 분석 |
| 대상 파일 | `C:\_Hope\myoungyul\mysite\**`, `C:\_Hope\PaRaSOL\parasolv1\**` (읽기 전용) |

### 작업 내용

SPEC §1이 "명율에서 이식"이라 적은 항목들을 명율 코드베이스에서 직접 확인했다. **적혀 있는 것과 실재하는 것이 달랐다.**

### 변경 요약 — SPEC 수정 4건

| # | SPEC 원문 | 확정 | 근거 |
|---|---|---|---|
| 1 | MariaDB + `drizzle-orm/mysql2` | **PostgreSQL 17 + node-postgres** | 명율 스키마를 계승하지 않기로 이미 확정돼 MariaDB 명분 소멸. SPEC이 JSON 컬럼을 3곳 쓰는데 MariaDB JSON은 인덱싱 불가한 LONGTEXT. PaRaSOL과 스택 통일 |
| 2 | `sharp` 리사이즈 → 서버 저장 | **브라우저 Canvas 2종 생성 → FormData** | **명율에 sharp가 없다.** 실제는 `createImageBitmap` + Canvas + R2. 2GB 서버 메모리 경합 회피, 리눅스 바이너리 문제 소멸 |
| 3 | "명율 자동매물등록 이식" | **신규 개발** | 명율의 자동등록은 `naverScraper.ts`(Puppeteer 네이버부동산 스크래핑)이며 **본 프로젝트의 크롤링 금지와 충돌**. 텍스트 파서는 명율에 없음. 단 `server/db.ts:258`의 가격 문자열 파서는 재사용 가치 있음 |
| 4 | §3 타입 표기 | PostgreSQL 매핑 | `tinyint`→`smallint`, `json`→`jsonb`, 일시→`timestamptz` 등 |

또한 **PaRaSOL이 실제로는 Windows에서 빌드**하고 있음을 확인했다(WSL 미설치, `@next/swc-win32-x64-msvc`만 존재, 그런데도 릴리스 tar 존재). `next.config.ts` 주석의 "WSL2에서 빌드한다"는 실운용과 다르다.

### 이슈 / 특이사항

sharp 리눅스 바이너리를 Windows에서 받을 수 있는지 실측 검증함 — `npm i --os=linux --cpu=x64 --libc=glibc sharp`로 `@img/sharp-linux-x64` 획득 성공. 다만 Canvas 방식 채택으로 불필요해짐.

---

## 작업 #3 — 프로젝트 스캐폴딩 · tRPC 골격

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 (Phase 0-2, 0-3) |
| 대상 파일 | `src/app/**`, `src/server/trpc/**`, `src/trpc/**`, `next.config.ts` |

### 작업 내용

`create-next-app`(Next 16.3.0 · TS · Tailwind 4 · App Router · src/ · `--disable-git`)로 스캐폴딩. tRPC 11 + `@trpc/tanstack-react-query` 골격을 PaRaSOL 실물을 참조해 구성했다.

### 변경 요약

- `next.config.ts` → `output: "standalone"`
- **2계층 프로시저**: `publicProcedure` / `adminProcedure`. 비회원 전용이라 PaRaSOL의 고객용 `protectedProcedure`가 불필요
- `health.ping`(공개) + `health.adminPing`(관리자) — **배포 검증과 인증 경계 확인을 겸함**
- superjson을 서버 init·클라이언트 링크 양쪽에 지정
- 서버측 base URL을 `localhost` 대신 `127.0.0.1`로 — Node 18+가 localhost를 IPv6로 먼저 풀어 IPv4 바인딩 서버와 어긋나는 문제 회피

### 검증

`tsc --noEmit` ✅ · `eslint` ✅ · tRPC 왕복 `success`(**Date 타입 유지 확인** = superjson 정상) · `adminPing` **403 + 데이터 누출 없음**

---

## 작업 #4 — 코드값·스키마 (Phase 0-4)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 |
| 대상 파일 | `src/lib/codes.ts`, `src/db/schema.ts`, `src/db/index.ts`, `drizzle.config.ts`, `drizzle/0000_fresh_maelstrom.sql` |

### 작업 내용

SPEC §3을 그대로 옮기지 않고 **검토하면서** PostgreSQL로 이식. RULE-12에 따라 발견한 문제를 우회하지 않고 수정했다.

### 변경 요약 — SPEC §3에서 고친 것

| 원문 | 변경 | 이유 |
|---|---|---|
| `lat/lng decimal(10,7)` | `double precision` | 핵심 쿼리가 bounds 검색인데 numeric은 drizzle에서 **string**으로 읽혀 마커마다 parseFloat가 돈다 |
| `property_options` PK 없음 | **복합 PK** | 같은 옵션이 중복 삽입되면 상세에 "에어컨"이 두 번 뜬다 |
| `property_images.isMain` + `sortOrder` | **isMain 제거** | 대표 사진의 진실이 두 곳이 되어 `isMain=true`인데 `sortOrder=3`인 상태가 가능. **sortOrder=0을 대표로 통일** |
| `bjdCode char(10)` | `varchar(10)` | char는 공백 패딩으로 비교·조인 사고 |
| `site_settings.key/value` | `settingKey/settingValue` | RULE-10 |

### 없어서 채운 것

- **`properties.videoSummary`** — 디자인가이드 §5-7이 요구하는 자막 요약의 저장 자리가 없었음
- **`properties.videoThumbImageId`** — §5-7 "썸네일은 관리자가 지정". `property_images`를 가리키는 순환 FK
- **`property_logs.adminUserId`** — 과태료 소명자료에 "언제"만 있고 **"누가"가 없었음**
- **`inquiries/owner_requests.privacyConsentAt`** — 개인정보 수집 동의 증빙
- **`admin_users.failedLoginCount/lockedUntil`** — `/admin`은 공개 경로라 브루트포스 노출
- 코드값 5종(문의상태·의뢰상태·로그액션·옵션21·관리비3구분) + **인덱스 10종**(부분 인덱스 3개 포함)

### 일부러 하지 않은 것

1. **거래유형별 가격 CHECK 제약** — SPEC §5가 이미 "법정 필수항목 누락 시 저장 차단"을 등록 폼 책임으로 정했고, DB에서 막으면 작성 중인 HIDDEN 매물을 저장할 수 없다
2. **`areas` 테이블** — SPEC §2에 `/areas/[slug]` 라우트가 있으나 §3에 테이블이 없음. **테이블 추가는 나중에도 비용이 거의 없어** Phase 3으로 미룸. 반대로 컬럼 변경은 데이터가 쌓이면 비싸지므로 컬럼은 지금 다 잡았음

### 이슈 / 특이사항

`Set-Content -Encoding utf8`이 PowerShell 5.1에서 **BOM을 붙여 package.json을 깨뜨림**. Write 도구로 재작성해 복구. 이후 이 프로젝트의 파일 쓰기는 BOM 없는 방식으로만 진행.

---

## 작업 #5 — 서버 셋업 (0~10단계)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 인프라 |
| 대상 | `1.234.79.54` (카페24 가상서버, 호스트명 `ocy0027`) |

### 작업 내용

Ubuntu 24.04.4 LTS · RAM 1.9Gi · SSD 38G · **CPU 4코어** · **스왑 4GB 기존재**. 전 단계를 사용자가 직접 실행(RULE-2), Claude는 명령 작성과 외부 검증만 담당.

상세 절차와 함정 10건은 별도 문서: `계획_서버셋업(oh4989-server-setup)_20260805.md`

### 변경 요약

전용 계정 `oh4989` + SSH 키 인증 · swappiness 10 · **PostgreSQL 17.10**(PGDG) · DB/롤 + `pg_trgm` + `Asia/Seoul` · **2GB·4코어 튜닝**(shared_buffers 256MB, max_connections 30, 병렬 워커 제한) · 스키마 적용(테이블 8·FK 6·인덱스 10) · Node 22 + PM2(`User=oh4989`) · 배포 디렉토리 · **Nginx + Let's Encrypt**(HTTP→HTTPS, www→non-www **1회** 리다이렉트) · ufw(22/80/443만)

### 이슈 / 특이사항 (함정 10건 — 상세는 셋업 문서)

특히 시간을 쓴 것:
- **`sudo -u postgres psql` 실행 불가** — `/usr/bin/perl` 실행 권한이 비root에 막혀 있음(카페24 이미지 하드닝). psql 실체 바이너리 직접 호출로 우회
- **80/443이 밖에서 차단** — 서버 내부(ufw·iptables)는 깨끗한데 안 열림. **카페24 웹방화벽에서 개방 필요**. 서버 안만 봐서는 절대 안 보이는 원인
- **`pm2 startup`을 root로 실행** → `User=root` 서비스 생성. 재등록 시 `su -c` 방식은 `-u`가 빈 값이 되어 `pm2---hp.service` 생성. **root에서 `-u oh4989` 명시**로 해결
- **SSH 키 등록 실패** — `authorized_keys`가 0바이트. 파이프 방식이 빈 값을 흘림 → 공개키를 서버에서 직접 `echo`로 기록
- **채팅 복사 시 도메인이 마크다운 링크로 변환** → nginx `server_name` 깨짐. 설정은 저장소 파일로 만들어 scp

사용자 추가 조치: 카페24 웹방화벽에서 **22번을 특정 IP만 허용**. ⚠️ 배포가 22번을 쓰므로 개발 PC 공인 IP 변경 시 배포가 막힌다.

---

## 작업 #6 — 배포 파이프라인 · 첫 배포 (Phase 0-5)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 |
| 대상 파일 | `scripts/pack-release.mjs`, `scripts/deploy.mjs`, `scripts/server-deploy.sh`, `scripts/ecosystem.config.cjs`, `scripts/nginx-oh4989.conf` |

### 작업 내용

PaRaSOL의 로컬빌드→tar→a/b 슬롯 교체 파이프라인을 oh4989용으로 이식. **서버에서 빌드하지 않는다**(RAM 2GB OOM 위험).

### 변경 요약

- `pack-release.mjs` — 허용목록 방식 선별 복사 + **반출금지 검사**(.env/.ts/.md/tsconfig 하나라도 걸리면 tar 생성 중단)
- `server-deploy.sh` — a/b 슬롯 번갈아 사용, 항상 직전 버전 1개 보존, `./deploy.sh rollback`
- **자체 확인을 `robots.txt`가 아닌 `/api/trpc/health.ping`으로** — 정적 파일만 보면 "Next는 떴는데 서버 코드가 죽은" 상태를 놓친다
- `ecosystem.config.cjs` — `max_memory_restart: 400M`(PaRaSOL 500M에서 하향, PostgreSQL과 공존), `HOSTNAME: 127.0.0.1`
- Nginx `www-data`를 `oh4989` 그룹에 추가 + 경로 750/755 조정 — **안 하면 배포 후 CSS·사진이 전부 403**

### 검증 — 첫 배포 결과

```
빌드         27.8초 (TypeScript 8.9초)
반출금지     ✓ 소스·env 없음
tar          10.0MB
슬롯 전환     b → a
자체 확인     health.ping 200 (1초)
외부 확인     https://oh4989.com/api/trpc/health.ping → 200
총 소요       79초
```

운영 사이트 실측: HTTPS ✅ · tRPC 왕복 `success`(Date 타입 유지) ✅ · `--ink` = `rgb(15,28,46)`(#0F1C2E, 디자인가이드 §5-2 일치) ✅ · `--accent` = `rgb(31,78,140)`(#1F4E8C) ✅ · 가격 서체 Wanted Sans ✅ · `/_next/static/*.css` 200 ✅

### 이슈 / 특이사항

- **CRLF 함정** — Windows에서 만든 `.sh`를 리눅스에 올리면 `$'\r': command not found`로 죽는다. 모든 스크립트를 LF·BOM 없음으로 작성했고 바이트 단위로 검증함
- `sharp-win32-x64` 경고는 예상된 것 — `next`의 optional 의존성이라 담기지만 `next/image`를 쓰지 않아 호출되지 않음. **sharp 도입 시 이 경고가 실제 장애가 된다**

---

## 남은 임시 산출물 (RULE-13 점검)

| 파일 | 처리 |
|---|---|
| `src/app/page.tsx` | **임시 확인 화면** — 토큰·서체·tRPC 왕복 확인용. Phase 3에서 실제 홈으로 교체 예정. 파일 상단에 명시해 둠 |
| 스크래치 디렉토리의 `probe-bin.sh`, `nginx-oh4989-acme.conf`, `sharptest/` | 프로젝트 밖이라 방치 무해 |
| 서버 `~/0000_fresh_maelstrom.sql`, `~/nginx-oh4989*.conf` | 재적용 시 필요하므로 유지 |

프로젝트 안에는 정리할 임시물이 없음.

---

# 오후 세션 — Phase 1 구현 · 관리자 UI 개편 · 배포

> 계획: `계획_Phase1구현(oh4989-phase1-impl)_20260806.md` (Task 1~13 + T14 추가)

## 작업 #7 — Phase 1 전체 구현 (T1~T12)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 |
| 대상 파일 | `src/domain/**`, `src/server/auth·services·trpc/**`, `src/lib/image-resize.ts`, `src/app/admin/**`, `src/app/properties/[id]/**`, `src/app/uploads/**`, `src/app/api/admin/**`, `scripts/hash-password.mjs`, `vitest.config.ts` |

### 변경 요약

- **도메인(TDD, vitest 33개)**: 가격 `formatManwon`("4억 3,000")·`formatPropertyPrice`("500/45") / 층 §8 3분기(`3/15층`·`총 2층`·상가 저중고 차단, 지하 B표기, 토지 "-") / 면적 평 병기 / `propertySaveSchema` — 거래유형별 가격·건물유형별 층·주거형 방·욕실·방향, **HIDDEN은 법정 검증 유예, 공개(ACTIVE·COMPLETED)만 강제**
- **인증**: jose HS256 쿠키(`oh4989_admin`, 8h) + bcrypt + 5회/15분 DB 잠금. adminProcedure는 JWT만 판독(DB 재확인 없음 — 계정 비활성화 기능 부재 근거)
- **이미지**: 클라 Canvas 2종(1600/480, 전부 JPEG 재인코딩 = EXIF·GPS 소멸) → FormData 쌍 업로드 → `{hex24}.jpg`+`-thumb.jpg` 저장, 경로 이탈 방어 단일 지점, 서빙 라우트(운영은 nginx 선점)
- **CRUD**: 서비스가 트랜잭션+`property_logs` 소유(CREATED/UPDATED/STATUS_CHANGED/DELETED, from→to 스냅샷). 이미지 diff는 커밋 후 파일 삭제(롤백 시 파일 유실 방지). `completedAt`은 COMPLETED 진입 시각 보존
- **공개 라우터**: detailAddress를 **select 목록에서 원천 제외**(응답 후 strip 아님). ACTIVE·COMPLETED만, viewCount 증가, related 3건
- **공개 상세**: §4-P3 블록 순서 전부 + §8 법정 표 전 항목 + 관리비 3구분 + 거래완료(배지+취소선) + 영상 임베드. 동적 SSR(ISR은 Phase 4) — COMPLETED 즉시 반영 자연 충족
- tRPC `onError` 서버 로깅 추가(cause까지) — 운영 인프라 장애 진단용. `siteSetting.officeInfo` 공개 프로시저(RULE-14 레이어 준수)

### 검증 (전부 실측)

vitest 33/33 · tsc · eslint · build 통과. 로그인 403→**200** 경계, 업로드→서빙 200, 매물 3건 등록, 층 3분기 실측, **공개 응답 detailAddress 0건**, 가격 없는 ACTIVE 저장 400 차단, HIDDEN 404, 폼 동적 분기(단독=층 숨김·상가=저중고 숨김·월세=보증금+월세), 수정 프리필

## 작업 #8 — 관리자 UI 개편 (T14, 세션 중 추가 요청)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가(UI) |
| 대상 파일 | `admin/_components/AdminShell·AdminNav·FormKit·PropertyForm·ImageUploader·MaintenanceFeeEditor·LogoutButton`, `admin/(authed)/**`, `admin/login` |

**와꾸 = PaRaSOL 관리자 셸 목업 규격**(다크 사이드바 248px·900px 분기·sticky 상단바·드로어 276px·미니 법정 푸터 — `_핸드오프/PaRaSOL 브랜드 메인 화면_관리자만참조/` 실측), **콘텐츠 스타일 = 명율 FormKit**(SectionCard·ChipGroup·Stepper·FieldLabel, #101828 그레이+#2563eb) 이식. shadcn 없이 순수 Tailwind, 신규 의존성은 lucide-react 1개. 대시보드(KPI 4장+최근 등록) 신설. **필드 구성·법정 검증은 oh4989 기획 그대로**. `/admin`은 리스킨 비대상이라 팔레트 하드코딩이 의도임을 파일 주석에 명시

## 작업 #9 — 배포 · 운영 env 장애 해결

| 항목 | 내용 |
|------|------|
| 작업 유형 | 배포/트러블슈팅 |

배포 36초(a/b 전환) 성공했으나 **운영 DB 연결 실패**. 원인 실측 경로: 로컬 `Failed query` → 터널 리스닝 확인 → oh4989 DB \dt 8테이블 확인(사용자 psql이 postgres DB를 보고 있었음) → onError 로깅으로 **`getaddrinfo ENOTFOUND/EAI_AGAIN base`** 확정 = **로컬·서버 양쪽 env의 DATABASE_URL 줄이 SESSION_SECRET 붙여넣기 때 `-base64` 조각으로 손상**. 로컬 수정 → 서버 `shared/oh4989.env` 수정 + `current/.env.production` 반영 + pm2 restart로 해결. 운영 NOT_FOUND/404/200 정상 실측

### 이슈 / 특이사항

- Phase 0의 "스키마 적용됨" 기록은 정확했다 — 혼선의 원인은 psql 기본 접속이 postgres DB였던 것
- **배포 스크립트에 env 사전 검증(DATABASE_URL 호스트 형식) 추가 제안** — 다음 세션 개선 후보
- 검증용 [테스트] 매물 3건은 운영 DB에 HIDDEN으로 보존(사용자 결정) — 실검수 후 삭제 예정
- `.idea/dataSources.xml`이 스테이징에 있음 — 공개 리포면 gitignore 권고(사용자 판단)

## 작업 #10 — 네이버부동산 참고문서

`참고_네이버부동산_매물표기(oh4989-naver-ref)_20260806.md` — 실매물 스크린샷 13장 문자화. **발견**: ① 주력=상가·오피스텔 월세(332건 중 월세 244) ② officeInfo 실데이터(INSERT SQL 전달) ③ 스키마 갭 — 권리금·현재/추천업종 3컬럼 **RULE-12 제안** (채택 대기)

---

## 작업 #11 — 네이버 지도 전환 확정 · 상가 3컬럼 · Phase 2 서버 절반 (T15)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 확정 반영 + 기능추가 |
| 대상 파일 | `CLAUDE.md`, `AGENTS.md`, `.env.example`, `src/db/schema.ts`, `drizzle/0001_great_forgotten_one.sql`, `src/domain/{price,property-schema,map}.ts`, `src/server/{services/property.service,trpc/routers/property}.ts`, `admin/_components/PropertyForm.tsx`, `edit/page.tsx`, `properties/[id]/page.tsx`, `scripts/server-deploy.sh` |

### 변경 요약

- **[SPEC 수정 이력 #5] 지도: 카카오맵 → 네이버 지도(NCP Maps)** — 사용자 확정. 근거: 카카오 2026.7.21 "계정당 첫 앱만 무료"가 리스킨 전제와 충돌 / 네이버 월 1,000만 무료 / 네이버부동산과 시각 일관성 / 명율이 네이버 기반이라 실전 코드 참조 가능. env 키 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID`·`NAVER_MAP_CLIENT_SECRET`로 교체
- **상가 3컬럼(RULE-12 채택)**: `premium_fee`(0=없음 표기·null=미기재)·`business_type_current`·`business_type_recommended`. zod 선택 입력, 폼은 상가·사무실에서만 "상가 정보" 그룹 표시(실측: 아파트X·상가O·사무실O·토지X), 공개 상세 법정 표에 조건부 행, `formatPremiumFee` 도메인 함수(+테스트 34개째)
- **`property.mapSearch`** (Phase 2 서버 절반, 키 무관): bounds+zoom+유형 필터. `MAP_MARKER_MIN_ZOOM=14` 미만 동별 집계(count·avg 좌표), 이상 개별 마커 상한 200+truncated 신호, ACTIVE만, hasVideo 플래그(§5-5 ▶도트). 실측: 마커/동집계/숨김 반영 3케이스 통과
- **`property.list`** (Phase 3 목록 서버): id desc 커서 무한스크롤(20건+nextCursor), ACTIVE+COMPLETED(완료도 표시가 원칙), 대표 썸네일 일괄 조인
- **배포 스크립트 env 사전검증**: DATABASE_URL 형식·SESSION_SECRET 길이를 배포 시점에 검사(값 미출력) — 오늘 장애 재발 방지. LF·BOM·문법 검증 완료

### 이슈

- **ALTER 미적용 상태** — `detail` 계열은 서버 DB에 3컬럼 ALTER가 실행돼야 동작(additive라 기존 운영 코드는 무영향). 적용 후 재배포 필요

---

## 작업 #12 — 문의 기능 전체 (T16 — Phase 3 항목 선행)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 |
| 대상 파일 | `src/server/services/inquiry.service.ts`, `src/server/trpc/routers/{inquiry,admin-inquiry}.ts`, `_app.ts`, `src/app/inquiry/{page,InquiryForm}.tsx`, `properties/[id]/page.tsx`(CTA), `admin/(authed)/inquiries/page.tsx`, `AdminNav.tsx`, 대시보드 |

### 변경 요약

- **공개 접수** `inquiry.create`: zod에 `privacyConsent: literal(true)`(동의 강제) + 전화번호 형식. **레이트리밋 IP당 시간당 5회**(SPEC §4)를 DB count로 판정 — 재시작에도 유지, `inquiries_ip_recent_idx` 활용. `privacyConsentAt` 증빙 시각 기록(§5-8)
- **/inquiry 페이지**: §5 토큰, 동의 안내문(수집 항목·목적·보유 기간), `?propertyId=`로 매물 연결, officeInfo 전화 안내
- **상세 CTA**: "문의하기 (준비 중)" 비활성 버튼 → `/inquiry?propertyId=N` 실링크
- **관리자 문의함** `/admin/inquiries`: 목록·상태 4종(pill)·상담 메모·매물 링크·전화 링크. 대시보드 KPI를 "신규 문의"로 확장(전체 매물 카드와 교체), 사이드바 메뉴 추가
- 개인정보(이름·연락처)는 관리자 문의함에만 노출 — 공개면 어디에도 없음(RULE-11)

### 검증 (실측)

1~5회 접수 성공 → **6회째 TOO_MANY_REQUESTS**(레이트리밋 정확) · 동의 false → BAD_REQUEST · 문의함 5건(매물 연결·NEW·동의시각 기록) · newCount 5 · /inquiry 렌더. vitest 34 · tsc · eslint 클린

### 이슈

- **officeInfo INSERT 실행 확인됨** — /inquiry 하단에 ☎ 031-433-3119 표시 실측 (U5 완료)
- [테스트] 문의 5건이 DB에 남음 — 검수 후 문의함에서 "스팸" 처리 권장(하드 삭제 프로시저는 두지 않음)

---

## 작업 #13 — 등록의뢰 기능 + 공개면 법정 푸터 (T17)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 |
| 대상 파일 | `src/server/services/owner-request.service.ts`, `src/server/trpc/routers/{owner-request,admin-owner-request}.ts`, `src/app/request/{page,RequestForm}.tsx`, `admin/(authed)/requests/page.tsx`, `AdminNav.tsx`, `src/components/layout/SiteFooter.tsx`, 공개 3페이지(상세·문의·의뢰) |

### 변경 요약

- **등록의뢰(집주인용)**: `ownerRequest.create`(동의 강제·레이트리밋 5회/1h — 문의와 별도 카운트: 정상 사용자의 문의→의뢰 흐름을 막지 않기 위해) · `/request` 폼(주소는 대략만, 거래유형 토글) · 관리자 `/admin/requests`(상태 신규→연락함→매물 등록됨/반려, 메모) · 사이드바 메뉴
- **공개면 법정 푸터** `SiteFooter`: 명칭·소재지·연락처·등록번호·개업공인중개사 성명 5종(§8) — officeInfo(site_settings) 기반, 상세·문의·의뢰 3페이지 삽입. 중개보조원 필드는 두지 않음(표기 금지). 전 페이지 그룹화(route group 이동)는 Phase 3 홈 개편 때

### 검증 (실측)

의뢰 접수 성공 → 의뢰함 정합(주소 힌트·거래유형·NEW) · 동의 false 차단 · `/request`·`/inquiry` 법정 푸터 렌더. vitest 34 · tsc · eslint 클린

### 이슈

- **dev 캐시 함정**: `/inquiry` 수정이 touch로도 반영 안 됨(파일은 정상, Turbopack 캐시) — **dev 재시작으로 해결**. 같은 증상이면 재컴파일 의심 전에 파일 실상태 Read부터
- [테스트] 의뢰 1건 DB 잔류 — 검수 후 "반려" 처리 권장

---

## 작업 #14 — ALTER 반영 · 2차 배포 (운영 최신화)

- 사용자 ALTER 3줄 실행 확인 → 로컬 전 흐름 재실측: 상가에 권리금 0("없음")·현재업종 공실·추천업종 저장 → **공개 상세 법정 표에 표기 실측** → 원상 복구
- **2차 배포(81초)**: 상가 3컬럼 · mapSearch · 목록 서버 · 문의 전체 · 등록의뢰 전체 · 법정 푸터 · env 사전검증 — 운영 실측: detail NOT_FOUND 정상 / inquiry·request 200+푸터 / mapSearch 정상 / 문의 접수 성공(id 6)
- 사용자 피드백 반영: **SQL 전달은 psql 셸 래핑 없이 순수 쿼리로만** (DataGrip 콘솔 사용 — 메모리 기록)

## 작업 #15 — Phase 2 지도 화면 /map 1단계 (T18)

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능추가 (Phase 2 핵심 화면) |
| 대상 파일 | `src/components/map/{naver-loader,price-tag-marker}.ts`, `src/app/map/{page,MapClient,MapScreen,MapView,MapCard}.tsx`, `globals.css`(마커 CSS), `src/domain/map.ts`(초기 중심), `property.mapSearch`(썸네일 보강), `@types/navermaps` |

### 사용자 확정 (디자인가이드 대비 변경)

- **자동 재검색**: "이 지역 재검색" 버튼(§4-P1) 대신 **네이버부동산 방식** — idle 디바운스(400ms) 후 보이는 영역 자동 갱신. 리스트는 keepPreviousData로 리셋 없이 부드럽게
- **마커 UI**: §5-5 형태 구분(사각·꼬리·이중선·다이아) 대신 **네이버풍 카드 핀** — 상단 면적 뱃지 + 거래유형 접두("매/전/월/단") + 가격 + 하단 꼬리. 색은 토큰(매매=--ink·임대=--accent), 접두 글자가 색과 병행(색 단독 구분 금지 유지)

### 변경 요약

- **SDK 로더**: 명율 검증 패턴(ncpKeyId·스크립트 중복 방어·폴링) + 프로미스 캐시 + authFailure 훅
- **MapView**: 명율 성능 방식 계승 — **마커 id Map diff**(전량 재생성 금지), 아이콘 갱신은 강조 변화분만, idle 디바운스, stagger 등장(신규 20개·reduced-motion 준수), 명령 핸들(panTo)
- **MapScreen**: PC 2분할(360px 리스트+지도, hover↔마커 강조 연동) / 모바일(지도 전면+필터 칩 오버레이+바텀시트 3단 스냅(translateY만—리플로우 방지)+마커 탭 미리보기 별개 레이어) / 거래유형 멀티 칩 필터 / **상태 복원**(sessionStorage: 중심·줌·필터·선택·시트 — 상세 다녀와도 유지) / 줌 [+][−] 컨트롤
- 서버: mapSearch 마커에 리스트 카드용 필드(면적·층·동·썸네일) 보강

### 검증 (실측)

SDK 로드·인증 성공(키 정상) → 마커 3핀 렌더(네이버풍 면적뱃지+접두) → **마커 클릭 → 선택 강조+미리보기 카드(상세보기·전화·닫기)** → 월세 필터 → 1건 → 해제 복귀 → 스냅샷 복원(재진입 시 선택 유지 확인). **이중 마운트 버그 실측으로 발견·수정**(CSS 숨김 2레이아웃 → matchMedia 렌더 분기). vitest 34 · tsc · eslint · build(○ /map) 클린

### 남은 것 (2단계)

- PC 중앙 풀 상세 패널(§4-P1 580px — 현재는 좌하단 미리보기 카드+상세 링크)
- 가격·면적·관리비·옵션 드롭다운 필터 / 바텀시트 드래그 제스처(현재 탭 전환) / 공개 GNB·탭바(Phase 3 홈과 함께)
- 동 클러스터 시각 확인(서버·렌더 경로는 검증됨 — 실휠 줌아웃로 확인만)

---

## 작업 #16 — 울트라 리뷰 38건 수정 · 확정 핸드오프 반영 (T19~T20 진행분)

### 울트라 리뷰 (워크플로우 48 에이전트 — 6관점 파인더 + 발견별 적대적 검증)

**확정 38 / 기각 4.** 전량 수정 완료(핵심):
- **[critical] HIDDEN→ACTIVE 상태 전환이 법정 검증 우회** → `domain/publication-requirements.ts` 신설(스키마·상태전환 공유 단일 출처) + `updatePropertyStatus` 검사 + **실측 통과**(누락 필드 목록과 함께 BAD_REQUEST)
- **[critical] 방향은 건축물 공통 법정 필수**(상가·사무실 포함, 토지만 면제) — 면제 로직 수정 + 테스트
- 이미지 diff stale id → CONFLICT 거부(lost-update·대표사진 공백 방지) / 층 교차검증(floor>totalFloor·총층 0 차단) / 관리비 3구분 ACTIVE 강제 / **XFF 마지막 항목 신뢰**(레이트리밋 스푸핑 차단) / **로그인 차단을 (계정,IP) 단위로**(`admin_login_attempts` 테이블 — 계정 잠금 DoS 제거, 더미 해시 타이밍 방어) / viewCount를 `after()`로+updatedAt 보존 / mapSearch totalCount / 업로드 JPEG 매직바이트 / 세션 쿠키 decode 방어 / zod 에러 한국어 첫 메시지(errorFormatter) / 캐러셀 이전·다음 버튼 / 시트 inert·live region·라벨 / 필터칩 44px / 동의문구(거부권리·IP 고지) / 상세 직렬 왕복 축소(caller cache) / gcTime·memo·will-change 등
- 테스트 37개로 확장. 미반영: 카카오톡 경로(officeInfo 확장 — 백로그)

### 확정 핸드오프 도착 (`_핸드오프/Incomplete request/design_handoff_ohchaeyoung_realty/`)

**사용자 사이트 확정안 + 관리자 기획 + 지도 시안(baegot-map) + 로고·워드마크·실사진.** 이후 이 번들이 공개면·관리자의 기준이다(기존 디자인가이드 v1.1을 대체).
- **전역 토큰 교체 완료**: 딥네이비 `#0B2430` · 틸 `#146B7C` · 골드 `#E8C87A` · 크림 배경/보더 — globals.css @theme (전 화면 파급, 클래스명 유지로 무변경 전환)
- **지도 핀 확정 시안으로 교체 완료**: 흰 말풍선+거래유형 색점(매매/전세/월세 색맵)+압축가격(tnum)+꼬리, seen(방문 회색)·act(네이비 반전) 상태, 클러스터=틸 원+"N 매물". seen은 스냅샷에 저장·복원
- **0-크기 컨테이너 버그 실측 발견·수정**: 지도 컨테이너가 0px일 때 bounds가 한 점으로 붕괴 → 400 스팸. 가드 + ResizeObserver(SDK resize 트리거) 추가
- PC 3분할(리스트|상세패널|지도) 구현했으나 **확정안 PC2는 [조건패널 296px|목록|지도]** — 구조 재정렬 필요(상세는 별도 페이지). DetailPanel·PropertyDetailBody(상세 본문 공유 추출)는 유지 가치 있음(PC3 2단에 활용)

## 작업 #17 — 지도 확정 구조 완성 (M3 + PC2)

- **DDL 2건 적용 확인**: admin_login_attempts · officeInfo 확정값("Oh! 채영공인중개사무소"·278번길 26 — 실측)
- **모바일 M3**: 검색바(지역 사전 매칭 → panTo, 미매칭 시 건물명 keyword 검색) + [☰ 목록] 토글(#0B2430) + 칩(유형 그룹 3: 상가/아파트·오피스텔/사무실 + 거래 4 — 전부 실동작) + **시트 확정 스냅(peek 112px/half 48%/full 88%, .28s)** — half=가로 카드 레일(hcard 250px)·full=세로 리스트(vitem), 드래그+탭 겸용 + 현위치 FAB(geolocation). 마커 탭 → 시트 half + 레일 카드 센터링 + 핀 가림 방지 오프셋(-0.0016)
- **PC2 [조건패널 296px | 목록 360px | 지도]**: 검색란 맨 위(의뢰인 지시 — keyword는 서버 ilike 추가) → 건물 → 거래 → 지역 바로가기 → 조건 초기화. 목록 카드 클릭=핀 하이라이트, **재클릭=상세 이동**
- MapCard 확정 스타일 2형(list/rail — DEAL_COLOR 배지·17px 가격·tnum). 이전 회차의 DetailPanel(중앙 상세 패널)은 확정안에서 제외돼 미사용(파일 잔존 — 정리 후보), PropertyDetailBody는 PC3 상세 2단에서 재사용 예정
- 게이트: vitest 37 · tsc · eslint · build 클린. 시각 실측은 미리보기 패널 오픈 필요(0-크기 가드가 요청을 막아 안전)

## 다음 단계

- **사용자**: 미리보기 패널 열고 /map 최종 검수(모바일 시트 드래그·레일·PC 3분할) → **배포 승인**(미배포 누적: 확정 토큰·핀·지도 구조·리뷰 38건 수정)
- **다음**: 홈 M1/PC1(검색 우선) · 목록 M2 · 상세 M4/PC3 재정렬 · 비주얼 푸터(공공기관 링크) · 문의 M5(동의 2종 분리) — 전부 확정안 기준. 이후 관리자 기획 A1~A6(스키마: 계약중·중개보수 문구·진열순서·보관함·문의 자동파기 — RULE-12 일괄 정리)

---

## 작업 #18 — T21 확정안 홈(M1/PC1) · PC4 비주얼 푸터 · 사이트 크롬 + 울트라 리뷰 2차

- **작업유형**: 기능 구현(공개 홈) + 다각 리뷰·수정 + 실측 검증
- **대상파일**: `src/app/page.tsx`(교체) · `src/lib/brand.ts`(신규) · `src/components/home/*`(신규 5) · `src/components/layout/SiteHeader.tsx`·`SiteTabBar.tsx`(신규) · `SiteFooter.tsx`(PC4 개편) · `src/server/trpc/routers/property.ts`(homeSummary) · `src/server/services/site-settings.service.ts`(OfficeInfo 확장+법정 로그) · `src/app/map/MapScreen.tsx`(URL 딥링크) · `src/domain/map.ts`(키워드 상한 상수) · `public/brand/*`(자산 4종 변환)
- **작업내용**:
  1. 확정 핸드오프 기준 홈 구현 — M1(네이비 히어로+검색바·빠른진입 4타일 실건수·"오늘 새로 나온 자리" 레일·"추천 상가" 행·대표 신뢰 블록·영상 레일·하단 탭바) / PC1 S안(사진밴드 236px 히어로+검색 패널+유형 타일 6종 실건수+성과 3종/대표 행 → 오채영 PICK 밴드 → 새로 나온 상가 4열). 구어체 카피는 시안 원문 그대로 `brand.ts`에 고정.
  2. `property.homeSummary` — 최신 8·상가 4·영상 4 + count filter 집계 + 썸네일 합집합 1쿼리(총 5쿼리 고정).
  3. `/map` URL 딥링크 `?bt/dt/kw/rg/sheet` — 홈 타일·검색·헤더 GNB·탭바가 지도 필터로 직결.
  4. SiteFooter → 확정 PC4 비주얼형: 공공기관 바로가기 6종(등기소·정부24·세움터·토지이음·실거래가·일사편리, 시안 원문) + 4열(브랜드/사무소 정보/연락처/영업시간) + 바닥 바(©+등록·대표). 선택 필드는 officeInfo(jsonb) 확장으로 — 값 없으면 줄 숨김.
  5. **울트라 리뷰 2차(30 에이전트: 5관점 파인더+발견당 반박 검증)**: 25건 발견 → 18건 확정(중복 제외 실질 15) → **전량 수정**. 핵심: ① 딥링크가 렌더 중 `window.location`을 읽어 클라이언트 내비게이션에서 파라미터 유실(플레이키) → `useSearchParams`로 교체 ② 뒤로가기 시 URL 잔존 딥링크가 사용자 최신 필터를 덮음 → 스냅샷에 `appliedSearch` 기록해 동일 쿼리 재적용 차단(+동일 세그먼트 쿼리 변경은 렌더 중 상태 조정으로 재적용) ③ 무권리 집계가 권리금 미기재(null)를 무권리로 셈 → `premiumFee = 0`만(부당표시 차단) ④ 모바일 지도 half 시트에 법정 표기 부재 → 상시 렌더 ⑤ officeInfo null이면 법정 표기 없이 광고 지속 → 서비스에 error 로그+필드 검증 ⑥ 상호·성명 하드코딩 폴백 제거(alt는 brand.ts 자산 상수로) ⑦ 키워드 50자 상한 공유 상수화(조용한 검색 실패 차단) ⑧ 터치 타깃 44px(전체›·푸터 전화·✕) ⑨ 검색 input 포커스 링(form focus-within) ⑩ tablist→aria-pressed ⑪ ▶배지 sr-only ⑫ 히어로 fetchPriority ⑬ 워드마크 width/height(CLS)
  6. 실측 검증: tsc 0 · lint 0 · vitest 37/37 · 프리뷰(M 375/PC 1440) 전 섹션 렌더 · 딥링크 3종이 mapSearch 서버 쿼리에 실림(네트워크 실측) · **뒤로가기 가드 결정 실험 통과**(appliedSearch 일치 시 스냅샷 승) · 콘솔 에러 0.
- **이슈·결정(사용자/의뢰인 확인 필요)**:
  - PC1 검색 패널의 보증금·월세·면적 범위 필드는 mapSearch 서버 필터가 없어 **키워드 입력으로 대체** — 범위 필터는 지도 확장 반복에서 서버와 함께.
  - PC GNB가 확정안(상가/아파트/오피스텔/영상 매물/배곧 시세/소개)과 다르게 **상가/아파트/오피스텔/지도/상담 문의** — 대응 라우트 부재로 인한 적응. **의뢰인 확인 대상**.
  - 푸터 바닥 바의 개인정보처리방침·중개보수 안내 링크는 **대상 페이지 신설 전 보류**(보관 기간 등 정책 문구 대표 확인 필요 — 문의 M5 정비와 함께).
  - officeInfo 확장값(사업자·휴대폰·팩스·영업시간·유튜브·블로그) UPDATE SQL 전달 — **영업시간은 시안 두 곳 값이 상충(09:30–19:00 vs 평일 09:00–19:00·토 09:00–17:00)하여 대표 확인 필수**.
  - 리뷰 제안 중 미적용(제안으로 이관): 매물 상세 officeInfo null 게이팅, deploy.sh 등록번호 존재 grep 체크.

### 작업 #18 추가 — 4차 배포 (2026-08-07)
- officeInfo 확장 SQL 사용자 실행 확인 후 `npm run deploy` — 64초, a→b 슬롯 전환, health 200.
- 운영 실측: 새 홈 콘텐츠(히어로 카피·오채영 PICK·공공기관 링크)·푸터 확장값(사업자·팩스·영업시간·유튜브·블로그)·법정 표기·/map·/inquiry·브랜드 자산 전부 정상.
- 잔여 확인 항목: 영업시간 대표 확정(현재 09:30–19:00 게시), GNB 구성 의뢰인 확인, 개인정보처리방침 페이지(M5 정비와 함께).

---

## 작업 #19 — T22 확정안 매물 목록 M2 + 울트라 리뷰 3차 (2026-08-07)

- **작업유형**: 기능 구현(공개 목록) + 근본원인 디버깅 + 다각 리뷰·수정 + 실측 검증
- **대상파일**: `src/app/properties/page.tsx`·`ListScreen.tsx`(신규) · `src/server/trpc/routers/property.ts`(list 확장) · `src/lib/codes.ts`(BUILDING_GROUP_CHIPS 단일화·정렬 코드) · `src/domain/map.ts`(키워드 상한·escapeLikePattern) · `src/components/map/map-snapshot.ts`(신규 공유 모듈) · `src/app/map/MapScreen.tsx`(빈 키=명시적 해제 딥링크) · `SiteTabBar.tsx`('매물'→/properties)
- **작업내용**:
  1. M2 확정 구조: 상단 고정(검색바+지도 토글+필터 칩 행) → 총 건수+정렬(최신/가격 낮은/면적 넓은) → 세로 카드(사진 112×88·제목 14/800·가격 15/800 틸·스펙·배지, 거래완료=색+텍스트+취소선) → 무한 스크롤(IntersectionObserver). 목록↔지도가 같은 URL 문법(bt/dt/kw)을 왕복.
  2. list 프로시저: 정렬 3종 + 키워드 + totalCount + **(정렬값,id) 복합 커서**(가격 정렬 키=coalesce(매매가,보증금), 협의는 센티널 맨 뒤).
  3. **근본원인 2건 실측 해결**: ① tRPC 무한쿼리는 `initialCursor` 미지정 시 initialPageParam=undefined로 **영구 pending**(패키지 소스 실측) — initialCursor:null + zod cursor `nullish` ② 페이지 레벨 Suspense가 dev 스트리밍에서 완료 스왑 안 됨(hidden 템플릿 잔존 실측) — 동적 페이지라 Suspense 제거로 해소, 프로덕션 빌드 검증.
  4. **울트라 리뷰 3차(22 에이전트)**: 18건 발견 → 17건 확정 → 실질 클러스터 전부 수정: 터치 타깃 44px 일괄(칩·select·✕·폼·토글 — MapScreen 판례 준용), URL↔상태 양방향 동기화(탭 재탭=초기화, 자기 기록은 값 비교로 순환 차단), 정렬 URL 보존(sort= 목록 전용 키), LIKE 특수문자 이스케이프(escapeLikePattern — mapSearch·list 공용), **목록→지도 완전 동기화**(항상 bt/dt/kw 명시 + parseDeepLink "빈 키=해제" + 토글 시 스냅샷 appliedSearch 클리어), 옵저버 에러 무한루프 가드, isPlaceholderData 전환 표시, 칩 행 aria·포커스 링 패딩.
  5. 실측: 정렬 3종 순서·키워드·칩 필터·탭 재탭 초기화·sort URL 복원·잔존 스냅샷 해제 전이(최악 조건) 전부 통과. tsc 0·lint 0·vitest 37/37·build 0.
- **이슈·이연**:
  - **지도→목록 방향의 조건 유지는 미구현**(리뷰 [12], 사용자 확인 필요): MapScreen이 필터 변경을 URL에 안 쓰는 구조라, 완전한 왕복은 지도 URL 동기화 리팩토링 필요 — appliedSearch 모델과의 상호작용 재설계 포함. 상세 M4 반복에서 제안 예정.
  - 스크롤 위치 복원(확정안 "스크롤 위치 유지")은 브라우저 기본 복원에 의존 — 2페이지 이상 로드 후 복귀는 1페이지 기준이라 불완전. 백로그.
  - 기각 1건: ListRow memo 부재(20행 규모에서 비용 무의미).

---

## 작업 #20 — T23 확정안 상세 M4/PC3 재정렬 + 울트라 리뷰 4차 (2026-08-07)

- **작업유형**: 화면 재구성 + 신규 컴포넌트 + 다각 리뷰·수정 + 실측 검증
- **대상파일**: `src/components/property/PropertyDetailBody.tsx`(블록 분해 재작성) · `VideoEmbed.tsx`(신규 파사드) · `PhotoCarousel.tsx`(4:3 전환) · `src/components/map/MiniMap.tsx`·`MiniMapClient.tsx`(신규 미니뷰) · `src/app/properties/[id]/page.tsx`(M4/PC3 이중 조립) · `BackOverlay.tsx`(신규)
- **작업내용**:
  1. **M4 재정렬**: 캐러셀(4:3)+뒤로가기 오버레이 → 가격·제목(권리금 병기) → 조건 8칸 → 대표 코멘트("이 자리에 대한 제 생각" = description+대표 사진) → 지도 미니뷰 → 영상 → 옵션·관리비 3구분 → 법정 표 → 중개사무소. 하단 CTA = 전화 상담(틸 채움)+문자 문의(외곽, officeInfo.mobilePhone).
  2. **PC3 신설**: 좌 964px(캐러셀→8칸 스펙→코멘트→지도 h-80→영상→법정) + 우 372px sticky(가격 요약→연락 카드(전화/문자/카톡조건부/폼 링크)→중개사무소→비슷한 매물). PC만 SiteHeader.
  3. **지도 미니뷰**: 네이버 SDK ssr:false, 가격핀 1개(확정 핀 스타일), scrollWheel off.
  4. **울트라 리뷰 4차(18 에이전트)**: 14건 발견 → 12건 확정 → 11건 수정: 유튜브 파사드(이중 임베드 ~1MB 차단+숨김 시 자동 언마운트), BackOverlay Navigation API canGoBack, MiniMap 실패 복구(attemptKey 파생)+줌컨트롤 제거(role=img 충돌)+대비, SpecGrid dl 유효 마크업+8칸, 거래완료 배지 대비 7.6:1, PC 헤딩 순서(aside DOM 선행+grid 배치), 캐러셀 4:3.
  5. **함정 실측 2건**: ① 이 브라우저 패널은 컴포지팅 프레임이 없어 RO/IO 콜백이 발화하지 않음 — "옵저버 미발화"를 브라우저 한계로 오판할 뻔함. 미니맵은 **동기 크기 검사(즉시) + IO(전환·스크롤, 실브라우저)** 이중화로 확정 ② dev CDP 콘솔 버퍼는 console.clear·서버 재시작에도 잔존 — HMR 시점 에러(deps 6→7)가 계속 재출력되는 착시.
  6. 검증: tsc 0·lint 0·vitest 37/37·build 0. 실측: M4 순서·CTA 44px(tel/sms)·미니맵 핀 "5000/320"·PC3(aside DOM 선행+우측 시각 배치·8칸 dl·sticky 372px)·숨김 브랜치 지도 미초기화.
- **이연(백로그)**: PC 갤러리 썸네일 스트립(확정 [12], minor — 실사진 등록 후 상세 고도화 반복에서), 대표 코멘트 전용 컬럼(RULE-12 — 관리자 A1~A6 스키마 일괄 때 description 분리 제안).

---

## 작업 #21 — T24 확정안 문의 M5 + 개인정보처리방침 + 울트라 리뷰 5차 (2026-08-07)

- **작업유형**: 기능 구현(공개 폼·법정 페이지) + 스키마 확장 + 다각 리뷰·수정 + 실측 검증
- **대상파일**: `src/app/inquiry/InquiryForm.tsx`(전면 개편)·`page.tsx` · `src/app/privacy/page.tsx`(신규) · `src/db/schema.ts`(inquiries 4컬럼) · `drizzle/0003_previous_the_renegades.sql` · `src/server/services/inquiry.service.ts` · `src/server/trpc/routers/inquiry.ts`·`owner-request.ts` · `src/server/trpc/init.ts`(errorFormatter) · `src/lib/codes.ts`(INQUIRY_KINDS) · `SiteFooter.tsx`(방침 링크) · `RequestForm.tsx`
- **작업내용**:
  1. **문의 M5 확정 구조**: 2시간 회신 카피 → 이름·연락처 → **문의 유형 칩 3종**(매물 찾기/내놓기/기타) → 관심 지역·매물 유형 → 내용(선택) → **동의 2종 분리** → h50 틸 제출 → "전화가 더 빠릅니다" 행(전화·카톡).
  2. **스키마 확장(DDL 사용자 실행 완료)**: `inquiry_kind`·`interest_dong`·`interest_building_type`·`marketing_consent_at` 4컬럼. 광고성 동의는 **별도 시각 컬럼**으로 증빙 분리.
  3. **개인정보처리방침 `/privacy` 신설**: 수집·목적·보유(3개월) 표 3행, 제3자·위탁 없음, 파기, 정보주체 권리, 보호책임자(officeInfo), **안전성 확보조치·권익침해 구제·변경 고지**. 푸터 바닥 바·양 폼의 "전문" 링크 연결.
  4. **울트라 리뷰 5차(18 에이전트, 1차 연결오류 후 resume)**: 15건 발견 → 9건 확정 → **전량 수정**. 핵심: ① **INTERNAL_SERVER_ERROR 메시지 마스킹**(tRPC가 비-TRPCError를 감쌀 때 cause.message를 상속 → pg의 `column ... does not exist`·`ECONNREFUSED 호스트:포트`가 공개 폼 빨간 알림에 그대로 노출되던 것을 errorFormatter에서 일반 안내문으로 치환. 공개·관리자 전 화면 동시 보호) ② **[선택] 광고성 동의에 목적·보유·거부권 고지 추가**(개보법 §22 구분 고지 — 필수만 고지하고 선택은 한 줄이던 상태) ③ 고지 항목과 실제 저장 항목 불일치 정정(관심 지역·유형 누락) ④ 법정 고지 캡션 대비 2.64:1 → 5.52:1 ⑤ 방침 법정 필수 기재 3건 보강 ⑥ "전문" 링크 44px·새 탭(같은 탭 이동 시 작성 내용 소실) ⑦ 내용 필드 시안대로 선택 항목화 ⑧ 상한 초과 시 zod 영어 문구 → 한국어(문의·의뢰 양쪽) ⑨ 의뢰 폼에 방침 링크.
  5. 검증: tsc 0·lint 0·vitest 37/37·build 0. **DB 관통 실측**: 신규 컬럼 4종 저장(id 7)·내용 없이 접수(id 8)·41자 초과 한국어 안내·방침 9개 절·동의 2종 각각 고지·전문 링크 44×44 새 탭.
- **이슈**:
  - ⚠️ **검증용 테스트 문의 2건(id 7·8) DB 잔존** — 사용자 정리 필요(`DELETE FROM inquiries WHERE id IN (7,8);`).
  - 관리자 문의함은 아직 신규 필드(유형·관심 지역·광고 동의)를 표시하지 않는다 — 관리자 A4 반복에서 반영.
  - 문의 3개월 자동파기 배치는 미구현(방침이 먼저 공표됨) — 관리자 A4와 함께. **오픈 전 필수**.
  - 기각 6건: 관리자 문의함 42703(DDL 적용으로 해소)·interestDong 자유문자열(select 고정)·광고 동의 보유 충돌(별도 컬럼)·유형 칩 radiogroup·관심 매물 자동채움·탭바 vs 고정 CTA.

---

## 작업 #22 — T25 관리자 스키마 일괄 정비: status 2축 분해 + 코드 전면 전환 (2026-08-07)

- **작업유형**: 스키마 설계(멀티에이전트 분석) + 전면 리팩토링 + 검증
- **분석**: 관리자 기획 A1~A6 전수 분석 워크플로(4갈래: A3 폼·A1/A2/A4/A6·법정/정책·코드 영향 → 종합). 제안서 `docs/project/계획_관리자스키마정비(oh4989-admin-schema)_20260807.md`. **사용자 결정**: 전체 승인 · 진열순서 공개 정렬 반영 · 네이버 매물번호는 화면만(수기).
- **스키마 변경(코드 반영 완료, DDL 사용자 실행 대기)**:
  - properties: `status` → **`listing_visibility` + `deal_progress` 2축 분해**('계약중'이 처음으로 저장 가능) + `display_order`·`price_negotiable`·`broker_fee_note`·`listing_code`·`naver_listing_no`·`map_pin_mode`·`exposure_checked_at`·`archived_at`. status 컬럼은 Phase 2(DROP)까지 deprecated로 스키마에 유지(스냅샷 정합).
  - inquiries·owner_requests: name/phone **NOT NULL 해제**(3개월 파기 가능화) + `contact_purged_at` + `inquiry_source`.
  - 신규 테이블: `inquiry_logs`(A4 연락 이력) · `property_view_daily`(A1 주간 조회 — 유일하게 백필 불가라 즉시 도입).
  - admin_users: `display_name` + `admin_role`.
  - 부분 인덱스 3종 재작성(`properties_public_*` — 술어 listing_visibility+archived_at) + listing_code 부분 UNIQUE.
- **코드 전환(14파일)**: codes.ts(2축·핀모드·문의 경로/이력·권한 코드) · property-schema+publication-requirements(협의 분기·중개보수 필수) · property.service(**법정 게이트를 "전환 결과" 기준으로** — 위험 B-1 반영, 축별 이력 분리, 스냅샷에 description·brokerFeeNote 추가) · admin-property(updateExposure) · 공개 property 라우터(공통 `publiclyVisible` 술어 — B-2 인덱스 함의 보장, 지도=완료 제외·계약중 포함, LATEST 정렬 displayOrder 우선+커서 확장, **DONG_CENTER 좌표 치환**(`publicCoordinate` — 실좌표 비노출), 일자별 조회 upsert) · 공개 UI(dealProgress) · 상세 법정 표에 중개보수 행 · 관리자 대시보드/목록(2축 뱃지·듀얼 셀렉트) · 문의/의뢰함(파기됨 표기) · PropertyForm(2축 칩·진열·협의·중개보수·네이버번호·핀모드) · edit 캐스팅(B-3).
- **검증**: vitest 39/39(협의 허용·중개보수 게이트 신규 2케이스) · tsc 0 · lint 0 · build 0. drizzle 0004 생성(스냅샷 정합용 — 실행은 수기 Phase DDL).
- **주의(진행 순서)**: ① Phase 1 DDL 실행 → **즉시 배포**(사이 창에서 관리자 상태 변경 시 두 축 미갱신) ② Phase 2(DROP status)는 배포 후 ③ inquiry_logs 백필 INSERT는 1회만(멱등 아님) ④ 파기 배치·A4 화면은 다음 태스크.

### 작업 #22 추가 — 5차 배포 (2026-08-07)
- Phase 1 DDL 사용자 실행 → 로컬 실측(2축 백필 매핑 ACTIVE→VISIBLE+AVAILABLE·신규 필드 응답·detailAddress 비노출) → **즉시 배포(54초, b→a)** — DDL·배포 사이 창 최소화 원칙 이행.
- 운영 실측: 홈·목록·상세·지도·방침·문의 전부 200, list 2축 응답, admin 403 게이트. **T22(목록)+T23(상세)+T24(문의·방침)+T25(2축)가 이번 배포로 일괄 운영 반영.**
- 다음: Phase 2(DROP status) 사용자 실행 → schema.ts deprecated 제거+재generate. Phase 3(admin_users 잠금 컬럼 DROP)은 별도 승인.

---

## 작업 #23 — T26 관리자 A0 셸 + A1 대시보드 + A2 매물 관리 (2026-08-07)

- **작업유형**: 기획 스펙 추출(멀티에이전트) + 관리자 화면 구현
- **선행**: Phase 2 DDL(DROP status) 사용자 실행 → schema.ts deprecated 정의 제거 + drizzle 0005 재생성(스냅샷 정합).
- **스펙 추출**: 관리자 기획 dc.html에서 A0~A4 5화면 확정값(구조·픽셀·색·문구·renderVals)을 5에이전트 병렬 추출 → `docs/project/참고_관리자화면스펙(oh4989-admin-spec)_20260807.md`(88KB).
- **A0 공통 셸**: 사이드바 208px `#0B2430`(워드마크+ADMIN 골드 배지·항목 h38·활성 rgba(255,255,255,.12)·**상담 문의 골드 건수 pill**·하단 프로필+로그아웃) + 상단바 h56 흰 배경(사이트 보기↗·매물 등록 CTA) + 본문 `#F1EEE8`. 메뉴는 **구현된 4개만**(영상·통계·설정은 2·3단계 — 죽은 링크 금지). `admin.auth.me` 추가(displayName 표시, 없으면 loginId).
- **A1 대시보드**: `admin.dashboard.summary` 신설(6쿼리 병렬 — KPI 4·오늘 할 일·주간 인기·최근 6). KPI 확정색(새 문의 #C0392B·만료 임박 #8A6A12·공개 중 #0B2430·주간 조회 #146B7C), 오늘 할 일 3유형(문의/만료/사진 없음 — 실데이터 기반, 없으면 "처리할 일 없음"), 주간 바 차트(view_daily 7일 합, 데이터 없으면 "도입일부터 쌓입니다" 안내), 최근 6장 그리드(숨김·거래완료만 배지).
- **A2 매물 관리**: `admin.property.list` 전면 확장(검색=매물명·주소·관리번호 / 종류·거래·공개상태·거래진행 필터 / **거래완료 자동 후순위→진열순서→최신** 정렬 / 보관함 탭 / 요약 카운트 / 썸네일 서브쿼리) + `bulkAction`(공개·숨김·거래완료로·보관/해제 — **건별 트랜잭션이라 법정 요건 미비 건만 실패로 반환**) + `renewExposure`. 화면은 확정 10컬럼 그리드(38/66/1fr/74/66/132/84/62/84/70)·h62 행·선택 행 #F6FAF9·일괄 띠 #0B2430·상태 배지 3색. **삭제 버튼 제거**(설계원칙 2 — 보관함으로 대체).
- **서비스**: `setPropertyArchived`(ARCHIVED/UNARCHIVED 로그) · `renewPropertyExposure` 추가.
- **검증**: tsc 0 · lint 0 · vitest 39/39 · build 0 · 관리자 게이트 403 · 서버 에러 0. (관리자 화면은 로그인 필요라 브라우저 실측은 사용자 몫)
- **남음**: A3 카드 8개 재배열, A4 문의함(타임라인·**3개월 파기 배치**), 울트라 리뷰, 배포.

### 작업 #23 이어서 — A4 상담 문의 + 연락처 자동 파기 (2026-08-07)
- **순서 변경 근거**: A3(폼 카드 재배열)는 이미 동작하는 폼의 배치 개선이고, A4에는 **법정 필수인 3개월 파기 배치**가 포함돼 A4를 먼저 구현.
- **파기 서비스** `inquiry-purge.service.ts`: 이름·연락처만 NULL로 지우고 행·이력은 보존(개인정보보호법 §21), `contactPurgedAt` 기록으로 멱등성+"파기됨 vs 미기재" 구분. 보유 기간은 `site_settings.inquiryRetention`(3개월) 단일 출처 — 방침 페이지 문구와 같은 값.
- **자동화**: `POST|GET /api/cron/purge-contacts` — `CRON_SECRET` Bearer 인증, **미설정이면 503으로 거부**(열린 파기 경로 방지). 서버 crontab 1줄로 하루 1회 호출. `.env.example`에 키·crontab 예시 추가.
- **A4 화면**: 좌측 목록 430px(탭 5종·신규 좌측 틸 마크+#F6FAF9) / 우측 상세(전화·문자 버튼, 문의한 매물 링크, 관심 조건, 본문, 처리 상태 칩 5, **연락 이력 타임라인+통화 메모 추가**, 내부 메모, **네이비 개인정보 보관 카드**(파기 예정일/파기 완료 표기·수동 파기 버튼)). 파기된 건은 이름 "(파기됨)"·연락처 버튼 자체가 사라진다.
- **라우터**: `admin.inquiry.list`(상태 탭 카운트·매물 조인·신규 필드 전부) · `logs` · `addLog` · `updateStatus`(상태 전환도 이력 기록) · `purgeExpiredContacts` · `purgeStatus`.
- **검증**: tsc 0 · lint 0 · vitest 39/39 · build 0 · 파기 엔드포인트 미설정 503 · 문의함 403 · 공개 홈 200.
- **남음**: A3 카드 8개 재배열(우측 레일 체크리스트 포함), 울트라 리뷰, 배포. 배포 후 서버에 `CRON_SECRET` 설정 + crontab 등록 필요.

### 작업 #23 이어서 — PC 홈 히어로 스케일 미세조정 (사용자 지적, 2026-08-07)
- **문제**: 확정 시안은 1464px 고정 프레임 기준이라 요소가 상대적으로 크게 보이는데, 실제 운영은 1900px+ 모니터에서 콘텐츠가 max-w-1168로 묶여 **폰트·히어로 세로가 작아 보임**.
- **조치(xl≥1280에서만 적용 — 시안 폭에서는 확정값 그대로 유지)**: 히어로 사진 밴드 236 → 286(xl)/320(2xl) · 히어로 카피 40 → **50px/-2px**(확정 토큰 PC 히어로 값) · eyebrow 12.5→13.5 · 서브 14.5→16.5 · 컨테이너 1168→1320 · 유형 타일 h76→88(라벨 13.5→15) · 성과 숫자 22→26 · 대표 한마디 13.5→15 · 검색 패널 필드/버튼 h46→52·탭 h36→40.
- **실측**: 1920 → 밴드 320·h1 50px·타일 88·성과 26px·패널 1272 / 1440 → 밴드 286·h1 50px, 가로 오버플로 없음. tsc·lint·build 0.

---

## 작업 #24 — 🔴 관리자 로그인 장애 핫픽스 + A3 카드 재배열 (2026-08-07)

- **장애**: 사용자가 관리자 로그인 시 "일시적인 오류가 발생했습니다…" 노출. **추측 없이 실측**(로컬에서 같은 요청 재현 → stack 확인): `Failed query: select "id","login_id",...,"failed_login_count","locked_until" from admin_users` — **Phase 3 DDL로 DROP된 컬럼을 schema.ts가 아직 선언**해 Drizzle이 select에 포함 → 42703 → INTERNAL_SERVER_ERROR → T24 마스킹 문구가 표시된 것. 운영도 동일 코드라 **관리자 로그인 전면 불가 상태였음**.
- **원인 구조**: `verifyAdminLogin`이 `.select()`(무인자)로 조회 → 스키마 전 컬럼 나열. DDL과 schema.ts 정의가 어긋나면 즉시 500. Phase 3 실행 시 정의 제거가 선행됐어야 함.
- **수정**: ① schema.ts에서 `failedLoginCount`·`lockedUntil` 제거(+drizzle 0006) ② INTERNAL_SERVER_ERROR 마스킹 문구에서 손님용 "전화로 연락해 주세요" 제거 — 공개면·관리자 공용 문구라 관리자 화면에 엉뚱하게 보였다.
- **검증**: 로컬 재현 → 수정 후 `UNAUTHORIZED / "아이디 또는 비밀번호가 올바르지 않습니다."` 확인 → **핫픽스 배포(25초)** → 운영에서 동일 응답 확인. 공개면 6종·로그인 화면 200, 파기 엔드포인트 503(미설정 안전 기본값).
- **A3 진행분**: 카드 목차 11항목으로 확정 순서 반영, **노출 제어 카드 분리**(3열 서브패널 + 진열 순서 **스테퍼**·프리셋 3), 가격 카드에 **목록 표기 요약 바** + **중개보수 프리셋 3종**(확정 원문), 우측 레일에 **공개 전 확인 체크리스트**(저장 게이트와 동일 함수로 판정 — 안내와 차단 사유 불일치 방지).
- **교훈(기록)**: 컬럼 DROP은 **코드에서 정의 제거 → 배포 → DROP** 순서가 안전하다. 반대로 하면 `.select()` 무인자 조회가 전부 터진다.

### 작업 #24 이어서 — 사이드바 홈페이지 바로가기 + 7차 배포 (2026-08-07)
- 사용자 요청: 관리자 좌측 **상단·하단에 홈페이지 이동 링크(아이콘 포함)**. 상단은 워드마크 아래 외곽선 버튼(Home + ExternalLink), 하단은 프로필 위 텍스트 링크. 둘 다 `target="_blank"` — 관리 작업 중이던 화면을 잃지 않게. 히트 영역 44px 유지.
- A3 진행분(노출 제어 카드·진열 스테퍼·중개보수 프리셋·목록 표기 요약 바·공개 전 확인 체크리스트) 포함 **7차 배포(28초)**.
- 운영 실측: 홈·목록·지도·로그인 화면 200, 로그인 경로 UNAUTHORIZED(정상 인증 응답).
- **깨진 글자 문의건**: 화면 문제 아님 — 검증용으로 넣은 테스트 문의 1건의 한글이 저장 시점에 깨진 것(다른 문의는 정상). 정리 SQL 사용자 전달.

---

## 작업 #25 — T26 A3 마무리 + 울트라 리뷰 6차 확정 18건 수정 (2026-08-07)

- **A3 마무리**: 매물명 강조 패널(액센트 바·필수 배지·글자수 n/60·h52 입력·작명 가이드) · 위치 카드 공개(틸)/비공개(적색) 대비 패널 · 좌표 카드 문구를 네이버 기준으로 정정.
- **울트라 리뷰 6차(27 에이전트)**: 23건 발견 → **18건 확정 → 전량 수정**.
  1. **A2 일괄 처리 대상 오류(major)** — 필터를 바꿔도 selectedIds가 남아 **화면에 없는 매물이 일괄 처리**되고 completedAt까지 오염될 수 있었다. `visibleSelectedIds` 파생값으로 "보이는 것만 처리"로 바꾸고 allChecked를 집합 판정으로 교정(핸들러마다 초기화 넣는 방식은 필터 추가 시 재발).
  2. **연락처 파기 미이행(major·법정)** — name/phone만 지워 **본문·상담 메모·연락 이력에 적힌 번호가 영구 잔존**했다. `maskPhonesInText`로 message·adminMemo·inquiryLogs.memo의 번호를 마스킹하고 `createdIp`도 함께 파기. 방침 페이지에 이 처리를 명시.
  3. **renewExposure 죽은 뮤테이션(major)** — 호출 UI가 없어 '노출 만료 임박' KPI를 영구히 못 지웠다. A2 행에 **연장 버튼**(만료 임박 행에만) 연결 + 판정을 서버 SQL(`isExposureExpiring`)로 옮겨 클라 시계 의존 제거.
  4. 주간 조회 집계 UTC/KST 어긋남 → `current_date` 기준으로 교체(구간 길이도 7일로 통일). 만료 임박 카운트에서 거래완료 제외.
  5. A2 체크박스 히트 영역 44px(그리드 첫 트랙 38→44) · 좁은 화면 조회수 라벨 · 종류 칩 10종 전체.
  6. 드로어 `role=dialog`+포커스 이동+ESC, 오버레이 중복 라벨 제거 · 등록/수정 화면에서 상단 CTA 숨김(작성 중 이탈 방지).
  7. 체크리스트 완료/미완료를 sr-only 텍스트로도 전달 · TimelineSection `key`로 문의 전환 시 통화 메모 초기화 · SECTION_LINKS 순서를 실제 카드 순서와 일치.
- **검증**: tsc 0 · lint 0 · vitest 39/39 · 공개 5종·로그인 200 · 관리자 게이트 403 · cron 503 · 서버 에러 0.
