@AGENTS.md

# CLAUDE.md — oh4989 (오채영부동산) 프로젝트 규칙

시흥 배곧 단일 공인중개사무소용 **지도형 매물 사이트**. 도메인 `oh4989.com`. 비회원 전용이며, 디자인 토큰 교체만으로 다른 중개사무소용 리스킨이 가능한 구조를 전제한다.

**이 사이트의 단 하나의 임무: 지도에서 매물을 찾아 전화·문의로 전환시키는 것.**

---

## [참조 문서] — 프로젝트 폴더 밖에 있음

| 문서 | 경로 | 저장소 |
|---|---|---|
| 개발 핸드오프 스펙 (기능·라우트·스키마·마일스톤) | `docs/handoff/SPEC_v1.md` | ✅ 포함 |
| 디자인 가이드 (토큰·레이아웃·법정 표기·금지사항) | `docs/handoff/디자인가이드_v1_1.md` | ✅ 포함 |
| 디자인 최종 확정안 (실행형 프로토타입) | `C:\_Hope\Ohsite\_핸드오프\확정안_프로토타입.html` | ❌ 17MB — PC마다 별도 배치 |
| 참조 리포 — 명율 (Vite+Express, 이식 원장) | `C:\_Hope\myoungyul\mysite` | ❌ PC마다 경로 다름 |
| 참조 리포 — PaRaSOL (Next16 구조·배포 원장) | `C:\_Hope\PaRaSOL\parasolv1` | ❌ PC마다 경로 다름 |

⚠️ 저장소에 없는 3개는 PC마다 위치가 다를 수 있다. 없으면 그 항목만 건너뛰고 진행한다 — 스펙과 디자인가이드는 저장소 안에 있으므로 작업에 지장 없다. 이전 절차는 [docs/PORTING.md](docs/PORTING.md).

- **확정안 HTML은 번들된 실행형 프로토타입(17MB)이다. 소스 참조용이 아니다** — 브라우저로 열어 화면을 확인하고 **재구현**한다. 복사 대상이 아니다.
- 화면 구현 착수 시: 디자인 가이드의 해당 절(§4 레이아웃 · §5 토큰/모션 · §8 법정 표기 · §9 금지사항)을 먼저 확인한다.
- 색·간격·서체 값은 디자인 가이드 §5가 단일 기준이다. 가이드가 비워둔 곳에서만 판단한다.

## [SPEC 수정 이력] — 원문보다 이 항목이 우선한다

`SPEC_v1.md` 원문과 아래가 충돌하면 **아래가 맞다.** (2026-08-05 실측 검증 후 확정)

| # | SPEC 원문 | 확정 | 근거 |
|---|---|---|---|
| 1 | MariaDB + `drizzle-orm/mysql2` | **PostgreSQL 17 + `drizzle-orm/node-postgres`** | 명율 스키마를 계승하지 않기로 이미 확정돼 MariaDB의 명분이 없음. SPEC이 JSON 컬럼을 3곳(`maintenanceDetail`·`site_settings.value`·`property_logs.snapshot`) 쓰는데 MariaDB JSON은 인덱싱 불가한 LONGTEXT. PaRaSOL과 스택 통일 |
| 2 | `sharp` 리사이즈 → 서버 저장 | **브라우저 Canvas에서 2종(상세·썸네일) 생성 → FormData 업로드 → 서버는 검증·저장만** | 명율에 sharp 파이프라인이 실재하지 않음(실제는 Canvas + R2). 2GB 서버에서 libvips 메모리 경합 회피, 리눅스 바이너리 문제 소멸 |
| 3 | "명율 자동매물등록 로직 이식" | **신규 개발** | 명율의 자동등록은 Puppeteer 네이버부동산 스크래핑(`naverScraper.ts`)이며, 본 프로젝트의 크롤링 금지 원칙과 충돌. 텍스트 붙여넣기 파서는 명율에 없음. 단 명율 `server/db.ts`의 가격 문자열 파서는 재사용 가치가 있음 |
| 4 | §3 타입 표기 | `bigint PK autoincrement`→`bigint generated always as identity` · `tinyint`→`smallint` · `json`→`jsonb` · 일시→`timestamptz` (`decimal`·`char(10)`·`varchar`는 그대로) | PostgreSQL 전환에 따른 매핑 |

---

## [스택]

Next.js 16 App Router (`output: 'standalone'`) · React 19 · TypeScript 5 · tRPC 11 + `@trpc/tanstack-react-query` · Drizzle ORM + **PostgreSQL 17** · Tailwind 4 · Node 22 · 카카오맵 JS SDK

⚠️ Next.js 16은 학습 데이터와 다를 수 있다 — 구현 전 `node_modules/next/dist/docs/` 및 [AGENTS.md](AGENTS.md) 확인.

**환경 구성**

- **개발**: 앱은 로컬(Windows)에서 실행 · DB는 서버 PostgreSQL에 **SSH 터널**로 접속(`127.0.0.1:15432` → 서버 5432). **5432 포트 외부 개방 금지.**
- **서버**: 카페24 가상서버 `1.234.79.54` — **RAM 2GB / SSD 40GB**. Ubuntu + Nginx + PM2 + Let's Encrypt. 전용 리눅스 계정 `oh4989` 아래에 격리하고 **root로 상시 작업하지 않는다.** swap 4GB 확보, PostgreSQL `shared_buffers`는 256MB 수준으로 낮춘다.
- **빌드**: **반드시 로컬에서 빌드한다. 서버 빌드 금지** — 2GB로는 Next 빌드가 OOM 위험이고, 빌드 중 운영 사이트가 느려진다. 빌드 산출물(standalone)만 tar로 반출한다.
- **배포**: `pack-release` → scp → 서버 `deploy.sh`가 a/b 슬롯 교체 → PM2 reload → 자체 확인 → 실패 시 `./deploy.sh rollback`. (PaRaSOL `scripts/` 이식)

---

## [RULE-1] 자격증명·환경설정 파일 접근 금지

**DB 접속 정보·API 키·서버 비밀번호가 포함된 파일은 읽지 않는다.**

접근 금지 대상:
- `.env` / `.env.*` (`.env.local`, `.env.production` 등)
- `C:\_Dev\_md_doc\conlab\projects\ocy\info.md` — 서버·카페24 자격증명 파일
- 참조 리포의 `C:\_Hope\myoungyul\mysite\.env`, `secrets\*.json`, `C:\_Hope\PaRaSOL\parasolv1\.env`

값이 비어 있는 템플릿(`.env.example`) 작성·수정은 허용. 설정이 필요하면 **키 이름만** 사용자에게 요청한다.

**실수로 읽었을 경우:** ① 즉시 사용자에게 알린다 ② 읽은 값(비밀번호·키·DB URL)을 응답에 옮기지 않는다 ③ 이후 해당 파일 접근을 거부한다.

## [RULE-2] DB 직접 접근 및 SQL 실행 금지

**메인 에이전트·서브에이전트 모두, 어떤 상황에서도 DB에 직접 연결하거나 DDL/DML/DCL을 실행하지 않는다.**

- 스키마 변경·데이터 조회가 필요하면 SQL을 **텍스트로 작성해 사용자에게 전달**한다. 사용자가 실행하고 결과를 알려주면 이어서 진행한다.
- Drizzle: 파일만 생성하는 `drizzle-kit generate`는 허용. **`push` / `migrate` 등 DB에 접속하는 명령은 금지.**
- 스킬이나 서브에이전트가 "꼭 필요하다"고 판단해도 예외 없다.

## [RULE-3] 허용 언어

**TypeScript / JavaScript / SQL(PostgreSQL) / HTML·CSS / bash(배포 스크립트)** 외의 언어로 코드를 생성하지 않는다. 다른 언어가 유리해 보여도 먼저 질문한다.

## [RULE-4] 계획 → 승인 → 실행 → 리뷰 → 확인

계획 없이 바로 코드를 쓰거나 파일을 고치지 않는다. 작업이 끝나면 변경 요약과 이슈를 보고하고 사용자 확인을 받는다.

## [RULE-5] 파일 삭제·이름변경·덮어쓰기는 승인 후

어떤 모드에서도 사전 승인 없이 실행하지 않는다. 무엇을 왜 바꾸는지 구체적으로 밝히고 묻는다. (신규 파일 생성은 승인된 계획 범위 안이면 진행 가능)

## [RULE-6] 모든 질문·설명은 한글로

기술 용어(컴포넌트명·함수명·경로)는 원문을 쓰되, 설명은 한글로 한다.

## [RULE-7] 형상관리(Git) 명령 실행·권유 금지

`git add`/`commit`/`push`, 브랜치 생성·삭제·병합을 실행하지 않고 권유하지도 않는다. 사용자가 명시적으로 요청한 경우의 `git log`·diff 조회만 허용. 요청 없이 `git status`나 파일 수정일자로 변경 내역을 간접 탐색하지 않는다.

## [RULE-8] 작업 로그

"작업내용 작성" 요청 시:
```
C:\_Dev\_md_doc\conlab\projects\ocy\worklog\작업로그_oh4989_YYYYMMDD.md
```
같은 날 파일이 있으면 이어서 추가(append). Markdown, Obsidian 연동. 항목: 작업유형 / 대상파일 / 작업내용 / 변경요약 / 이슈.

**프로젝트 문서 루트: `C:\_Dev\_md_doc\conlab\projects\ocy\`** — 계획·분석·위키 등 모든 문서를 이 폴더 하위에 작성한다.

## [RULE-9] 문서 작성 시 인덱스 등록

작업·분석 문서를 새로 만들면 아래 인덱스에 한 줄 추가한다. **이 등록은 RULE-5 예외로, 승인 없이 즉시 한다.**
```
C:\Users\TSline05\.claude\projects\C---Hope-Ohsite-oh4989\memory\MEMORY.md
```

## [RULE-10] 네이밍 — 대분류격 이름 자제

`type`·`data`·`info`·`code`·`status` 같은 상위 범주 단독 이름은 피하고 도메인을 한정한다 (`dealType`, `propertyStatus`, `carrierCode`). 특히 **tRPC 응답 필드는 그대로 JSON 키가 되므로** 더 구체적으로 짓는다. 범위가 넓을수록(전역 상태, 공통 함수 파라미터) 접두사를 붙인다. schema.ts 기존 컬럼명을 따라야 하거나 범위가 명확히 좁은 지역 변수는 예외.

---

## [RULE-11] oh4989 도메인 규약 — 위반 금지

**법적 리스크와 직결되는 항목이 섞여 있다. 우회 구현하지 않는다.**

**회원·개인정보**
- **비회원 전용.** 공개 화면(헤더·푸터·탭바 어디에도) 로그인·회원가입 진입점을 만들지 않는다. 관리자는 `/admin` 직접 접근.
- `detailAddress`(상세주소)는 공개 tRPC 응답에서 **항상 제거**한다. admin 라우터만 예외.
- 매물 사진은 **반드시 Canvas 재인코딩을 거친다.** 원본 직업로드 금지 — EXIF의 촬영 GPS가 실려 상세주소 비공개 원칙이 사진으로 새어나간다.
- 관심 매물은 localStorage에 **매물 ID 배열만** 저장. 이름·연락처 등 개인정보를 브라우저에 저장하지 않는다.

**데이터**
- 금액은 **만원 단위 정수**. 부동소수점·문자열 금지. 화면 표기는 한국식(`4억 3,000`, `월세 500/45`)이고 통화기호를 쓰지 않는다.
- 코드값은 `src/lib/codes.ts`의 `as const`만 참조한다. **DB ENUM 금지**(ALTER 잠금 회피).
- 상태 변경, 특히 `COMPLETED` 전환은 **반드시 `property_logs`에 기록**한다 — 거래완료 방치 과태료의 소명자료다. `completedAt` 설정 시 지도·목록·상세를 즉시 revalidate 한다.

**법정 표기 (공인중개사법 제18조의2)**
- 매물 상세에 디자인 가이드 §8의 명시항목을 **전부** 노출한다. 관리비는 일반/사용료/기타 **3구분** 필수.
- 건물유형별 층 표기 분기를 지킨다: 단독주택=총층만 / 그 외 주택=`해당층/총층`(저·중·고 대체 가능) / 상가·근생=`해당층/총층`(대체 불가).
- 중개사무소 법정정보(명칭·소재지·연락처·등록번호·개업공인중개사 성명)를 전 페이지 푸터와 매물 상세에 노출한다. **중개보조원 정보는 표기 금지.**
- **과장 문구 금지** — "최저가", "급매 확실" 등은 부당 표시·광고에 해당한다.

**구현 제약**
- 지도 컴포넌트는 `next/dynamic({ ssr: false })` 고정. 서버 렌더를 시도하지 않는다.
- **외부 플랫폼(네이버부동산·직방 등) 매물 크롤링 코드를 작성하지 않는다.**
- 자체 서버에 동영상을 저장하지 않는다. YouTube 일부공개 임베드만.
- PG·API 시크릿은 서버 env 원칙. 클라이언트 번들·`site_settings` 평문 저장 금지.

**접근성·브랜드**
- 색만으로 정보를 구분하지 않는다(거래완료 = 색 + 텍스트/취소선). 터치 타깃 44px 이상, 포커스 링 상시 가시, 아이콘만 있는 버튼 금지, `prefers-reduced-motion` 준수.
- 색·서체는 CSS 토큰, 상호·법정 표기·정책 문구는 `site_settings` — **리스킨 전제이므로 하드코딩하지 않는다.**

## [RULE-12] 스키마 개선 제안 의무 — 높은 우선순위

**운영 데이터가 쌓이기 전이 스키마를 고칠 수 있는 마지막 기회다.** 구현 중 어색한 구조·누락 컬럼·잘못된 타입·제약 부재를 발견하면 **애플리케이션 코드로 우회하지 말고 멈춰서 수정을 먼저 제안한다.**

제안 형식: ① 발견한 문제 ② 그대로 둘 때의 비용 ③ 수정안(스키마 diff + 마이그레이션 SQL) ④ 영향 범위. 사소해 보여도 보고하고, 채택은 사용자가 결정한다. **"나중에 고치자"를 금지한다.**

## [RULE-13] 임시 산출물 즉시 정리

검증·디버깅용 임시 스크립트, 일회성 테스트, 스캐폴드 잔여물은 목적을 다하면 정리한다(직접 지울 수 없으면 정리를 권유). 임시 파일이 필요하면 **프로젝트 밖**에 만든다. 작업 완료 리뷰 때 "이번에 생긴 임시 산출물이 있는가"를 항상 점검한다.

## [RULE-14] 모듈화·캡슐화 — 최고 우선순위

리스킨 판매가 전제다. 구조 품질을 기능 속도보다 우선한다.

- **레이어 경계 준수**: UI 컴포넌트 ↔ tRPC 라우터 ↔ 도메인 서비스 ↔ DB(Drizzle). 레이어를 건너뛰는 임포트 금지(컴포넌트에서 db 직접 임포트 금지).
- **도메인 로직은 화면에서 분리**: 가격 파싱·포맷, 법정 표기 분기, 상태 전이, 지도 bounds 계산 같은 규칙은 `src/domain`에 캡슐화한다 — 공개면·관리자·배치에서 재사용된다.
- **공개 표면 최소화**: 필요한 것만 export 한다.
- 단, **과잉 추상화와 구분한다** — 모듈화는 책임 분리이지 미래를 위한 추측성 유연화가 아니다. 사용처 1곳뿐인 코드의 추상화는 하지 않는다.

---

## 작업 전 체크리스트

- [ ] `.env` 계열이나 `ocy\info.md`를 열려 하는가? → **중단**
- [ ] DB 접속·SQL 실행을 하려는가? (`drizzle-kit push/migrate` 포함, 서브에이전트 포함) → **스크립트 작성 후 전달**
- [ ] TS/JS/SQL/HTML·CSS/bash 외 언어를 쓰려는가? → **먼저 질문**
- [ ] 계획 없이 구현하려는가? → **계획 먼저**
- [ ] 파일을 지우거나 덮어쓰려는가? → **승인 먼저**
- [ ] Git 관련 행동을 하려는가? → **조회 외 중단**
- [ ] 서버에서 빌드하려는가? → **중단, 로컬 빌드**
- [ ] 화면을 구현하는가? → **디자인 가이드 해당 절 + 확정안 프로토타입 먼저 확인**
- [ ] 금액·상태·주소·사진을 다루는가? → **RULE-11 확인**
- [ ] 스키마가 어색한가? → **우회 금지, 즉시 수정 제안**
- [ ] 임시 산출물이 남았는가? → **정리 또는 권유**
- [ ] 도메인 로직을 화면 코드에 넣으려 하는가? → **`src/domain`으로**
- [ ] "작업내용 작성" 요청인가? → `...\projects\ocy\worklog\작업로그_oh4989_YYYYMMDD.md`
- [ ] 문서를 새로 만들었는가? → **MEMORY.md 인덱스에 추가**
